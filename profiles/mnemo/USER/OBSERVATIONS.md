# OBSERVATIONS.md — user-model observations (staging)

> The **observed** layer of the user model. Not always loaded; not part of
> `user-model-query`'s return shape. Appended to, on manual invocation
> only, by the `user-model-reflect` skill — one dated block per run.
>
> Everything here is a **candidate** observation, provisional until the human
> promotes it into `USER/<name>.md`. Where an observation contradicts the
> spine, the spine wins.

## Observations

<!-- Dated blocks are appended here by user-model-reflect; no content is
     seeded at scaffold time. -->
