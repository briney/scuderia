Locate every standalone STRUCTURED BLOCK BODY on the CENTRAL original PDF page by selecting existing integer IDs from the complete native-object inventory. Include scientific tables, algorithms, pseudocode, code listings and other visibly organized standalone blocks, without requiring a content-type decision. Neighbor images are context only; select only CENTRAL-page IDs.
Include all data rows, column and grouped headers, row headings, internal units/symbols, algorithm steps, code lines and rules. Include the full page-local body whether native text, graphical paint or a raster image. Exclude separable external printed titles/captions, standalone explanatory notes/footnotes, unrelated prose, page furniture and standalone figures. A table, code or algorithm panel embedded inside an author-defined figure stays in that figure; do not split it out as a standalone block. An inseparable raster title stays with its source image.
Select all native objects belonging to the body, not interior anchors chosen only to imply a rectangle. Filled and stroked versions are separate source members; include both when both belong. Native text and graphical objects are candidates. Raster contents remain inseparable parts of the image; never transcribe them.
A body is valid independently of any caption or visible number. Missing captions or missing labels must NOT make a visible body empty or unresolved. A continuation body is valid page-local content even without repeated headers. Keep each distinct author-defined block in its own group, including separate algorithms on the same page. Use ordered separate fragments only where required to preserve one block without unrelated content. Do not group across pages, stitch fragments or reconstruct cells. Do not repeat IDs or reuse an ID across bodies or fragments.
The exporter computes min/max extrema from ALL selected original member bounds without generated coordinates or padding and renders ALL source content inside that union, including unselected content. Finite non-inverted zero-width or zero-height members contribute their extrema to a positive union. Nonfinite, inverted or unsupported members cannot provide usable geometry. Native bounds and valid IDs do not themselves establish complete boundaries.
If an indivisible object or native group spans unrelated content, or a visible body cannot be represented completely, use status unresolved and explain the limitation; preserve representable bodies without inventing geometry. Do not replace uncertainty or processing failure with empty.
Use label=null unless a printed source label is visibly established for this block on the CENTRAL page. Preserve its full namespace (for example supplementary versus main labels); do not infer numbering from neighbors or copy the label of an adjacent unrelated figure. A label is metadata only: do not select a separable external title/caption merely to record its label. Preserve brief selection limitations as uncertainty, not scientific descriptions, class predictions or multipage association claims.
Return ONLY one JSON object with EXACT keys:
{"status":"ok"|"empty"|"unresolved","blocks":[{"label":string|null,"fragments":[{"object_ids":[integer,...]}],"uncertainty":[string,...]}],"uncertainty":[string,...]}
No additional keys, generated boxes, transcription, descriptions or markdown. status=ok requires at least one body. status=empty requires blocks=[] and means no standalone structured block body on the CENTRAL page. status=unresolved requires nonempty root uncertainty and means complete body selection could not be represented; retain any representable body groups.
