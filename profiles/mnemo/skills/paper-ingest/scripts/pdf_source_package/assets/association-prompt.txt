Associate only the supplied source components within the single source document. Return a JSON object with exactly status, groups, unassociated, conflicts. No generated geometry, transcription, new candidate, new label, explanation, or additional property is allowed.

A group contains one or more body references and zero or more caption/note references. Bodies remain usable without captions. Captions or notes without an evidenced body association belong in unassociated. A note need not repeat the parent's label. Preserve existing figure membership; do not split panels. Do not merge standalone algorithms or other separately labeled elements merely because they are adjacent. Continuation across pages is permitted only when supported by the supplied source. Order all reference lists by physical page, top bound, left bound, then candidate ID.

Check the full printed namespace, including Figure versus Table versus Algorithm versus Data file, Extended Data or Supplementary prefix, and number/suffix. Equal numeric suffixes do not establish identity. An inline citation to a figure/table inside native text is not the component's own heading. Null labels are required where there is no source-observed label; never invent a title. An observed label whose origin is model-observation is a saved image observation, not native transcription. Type origin table-only-detector-unconfirmed identifies a provisional detector-family observation, not a confirmed table: a genuine source Algorithm heading can support an algorithm association without a table-versus-algorithm conflict. Other supplied types, including saved structured classifier decisions, retain their recorded provenance and are not native transcription. Do not group incompatible source namespaces or incompatible nonprovisional recorded types. Original page images follow the candidate list. If visual_context_mode is candidate-page-images-full-native, only candidate-bearing page images are supplied; all eligible native page text is supplied separately. This is reduced visual context, not unchanged all-page imagery. All text identified as native is literal original PDF text.

Every supplied candidate must occur exactly once across groups, unassociated, and conflicts. Use conflicts for ambiguous association, overlapping content, or insufficient source; do not silently emit the same scientific content twice when different detectors overlap. Do not reference another document or candidates outside this request.

Output shape:
{"status":"ok","groups":[{"source_document":"the supplied document ID","body_refs":["candidate ID"],"caption_note_refs":[],"label":null}],"unassociated":[{"candidate_id":"candidate ID","source_document":"the supplied document ID"}],"conflicts":[]}

A conflict has exactly {"candidate_refs":["candidate ID"],"source_document":"the supplied document ID","reason":"ambiguous-association"}; reason must be ambiguous-association, overlapping-content, or insufficient-source. Set status to unresolved when any conflicts remain. Do not omit candidates. No previous associations, human expectations, or reviewer comments are supplied.
