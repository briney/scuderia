Classify each supplied candidate's source content using its actual original-PDF crop images, literal native source text and original source-page/context images. Every candidate must receive exactly one decision. Keep candidates separate, even when several algorithms occur on one page; do not assign the title of one block to another. Page/context images can contain raster titles absent from native text and external captions outside a body-only crop.
Use content_type table, algorithm, code, other, uncertain or unknown. A table organizes scientific data or comparisons; an algorithm presents procedural or pseudocode steps; code is a program listing. Use uncertain or unknown with a brief reason rather than force an unsupported class. A printed title is evidence only when it belongs to this specific candidate. A nearby Figure caption does not name a standalone table; preserve full Figure/Table/Algorithm label namespaces. A table or code panel inside an author-defined figure should not be treated as a separate standalone extraction; mark the supplied candidate other or uncertain and explain the limitation.
Return source_label=null unless a printed label is visibly supported for this specific candidate; give its exact printed namespace and number without inventing labels. label_evidence is crop-image, context-image, native-text or none. This field records the model's observation, not an independently verified association. Use none exactly when source_label is null. Native text may be absent; do not require a caption or native title. Do not transcribe raster contents, generate descriptions, invent coordinates, merge candidates, reconstruct cells, stitch pages or change source geometry or role.
Return ONLY one JSON object with EXACT keys:
{"classifications":[{"candidate_id":string,"content_type":"table"|"algorithm"|"code"|"other"|"uncertain"|"unknown","source_label":string|null,"label_evidence":"crop-image"|"context-image"|"native-text"|"none","uncertainty":[string,...]}]}
No additional keys. Include every supplied candidate_id exactly once; no new IDs. uncertain and unknown require nonempty uncertainty. An empty uncertainty list for another class means no model-reported uncertainty, not independent correctness.
